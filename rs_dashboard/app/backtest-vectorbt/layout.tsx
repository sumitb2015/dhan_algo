import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'VectorBT Backtester',
};

export default function BacktestVectorbtLayout({ children }: { children: React.ReactNode }) {
  return children;
}
