import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Strategies Plus Monitor',
};

export default function StrategiesPlusLayout({ children }: { children: React.ReactNode }) {
  return children;
}
