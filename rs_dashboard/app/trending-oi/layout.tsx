import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Trending OI & Multi-Strike OI',
};

export default function TrendingOiLayout({ children }: { children: React.ReactNode }) {
  return children;
}
