import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Volume Footprint Order Flow',
};

export default function VolumeFootprintLayout({ children }: { children: React.ReactNode }) {
  return children;
}
