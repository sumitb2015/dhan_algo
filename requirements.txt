anyio==4.12.1
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
arrow==1.4.0
asttokens==3.0.1
async-lru==2.1.0
attrs==25.4.0
babel==2.17.0
beautifulsoup4==4.14.3
bleach==6.3.0
build==1.4.0
certifi==2026.1.4
cffi==2.0.0
charset-normalizer==3.4.4
colorama==0.4.6
comm==0.2.3
cryptography==46.0.3
debugpy==1.8.19
decorator==5.2.1
defusedxml==0.7.1
dhanhq==2.2.0
executing==2.2.1
fastjsonschema==2.21.2
fqdn==1.5.1
greenlet==3.3.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
iniconfig==2.3.0
ipykernel==7.1.0
ipython==9.9.0
ipython_pygments_lexers==1.1.1
ipywidgets==8.1.8
isoduration==20.11.0
jedi==0.19.2
Jinja2==3.1.6
json5==0.13.0
jsonpointer==3.0.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
jupyter==1.1.1
jupyter-console==6.6.3
jupyter-events==0.12.0
jupyter-lsp==2.3.0
jupyter_client==8.8.0
jupyter_core==5.9.1
jupyter_server==2.17.0
jupyter_server_terminals==0.5.4
jupyterlab==4.5.2
jupyterlab_pygments==0.3.0
jupyterlab_server==2.28.0
jupyterlab_widgets==3.0.16
lark==1.3.1
MarkupSafe==3.0.3
matplotlib-inline==0.2.1
mistune==3.2.0
nbclient==0.10.4
nbconvert==7.16.6
nbformat==5.10.4
nest-asyncio==1.6.0
notebook==7.5.2
notebook_shim==0.2.4
numpy==2.4.1
packaging==26.0
pandas==3.0.0
pandocfilters==1.5.1
parso==0.8.5
platformdirs==4.5.1
playwright==1.57.0
pluggy==1.6.0
prometheus_client==0.24.1
prompt_toolkit==3.0.52
psutil==7.2.1
pure_eval==0.2.3
pycparser==3.0
pyee==13.0.0
Pygments==2.19.2
pyOpenSSL==25.3.0
pyotp==2.9.0
pyproject_hooks==1.2.0
pyarrow>=19.0.0
pytest==9.0.2
pytest-base-url==2.1.0
pytest-playwright==0.7.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.1
python-json-logger==4.0.0
python-slugify==8.0.4
pywinpty==3.0.2
PyYAML==6.0.3
pyzmq==27.1.0
referencing==0.37.0
requests==2.32.5
rfc3339-validator==0.1.4
rfc3986-validator==0.1.1
rfc3987-syntax==1.1.0
rpds-py==0.30.0
Send2Trash==2.1.0
setuptools==80.10.1
six==1.17.0
soupsieve==2.8.3
stack-data==0.6.3
pandas-ta
tqdm>=4.67.1
terminado==0.18.1
text-unidecode==1.3
tinycss2==1.4.0
tornado==6.5.4
traitlets==5.14.3
typing_extensions==4.15.0
tzdata==2025.3
uri-template==1.3.0
urllib3==2.6.3
wcwidth==0.3.1
webcolors==25.10.0
webencodings==0.5.1
websocket-client==1.9.0
websockets==16.0
widgetsnbextension==4.0.15
xlsxwriter
kiteconnect
selenium
webdriver-manager
PyJWT==2.13.0

# Kotak Neo SDK (copy-trade child + selectable scalper broker).
#
# v3 (package "kotakneoapi", still imported as `neo_api_client`) supersedes
# the legacy git-pinned "neo-api-client" v2 fork this used to be. Migrated
# 2026-09-06 -- verified end-to-end against the live account (cached-session
# restore, a fresh totp_login()/totp_validate(), and real margin/positions
# reads all round-tripped correctly under v3).
#
# MUST be installed with --no-deps. It declares pandas>=2.2,<3 and
# numpy>=1.26,<3 -- installing it normally would downgrade this venv's
# pandas 3.0.0 to <3 and break the dashboard data stack (same failure mode as
# openstatz below). In practice pandas 3.0.0 works fine with it; the pin is
# just conservative packaging, unverified by us against 3.0.0 at the
# maintainer's end.
#
# Its real runtime needs beyond --no-deps are httpx[http2], websocket-client,
# websockets and PyJWT (all already above). The four below are pure-additive
# (no pandas/numpy conflict) so they install normally as part of this file;
# only the SDK itself needs the separate --no-deps command:
#
#   venv\Scripts\python.exe -m pip install --no-deps kotakneoapi==3.0.6
#
# kotakneoapi==3.0.6
h2==4.4.1
structlog==26.1.0
tenacity==9.1.4
python-decouple==3.8

# ── Backtest reporting (scripts/analysis/backtest_momentum_portfolio.py) ──────
plotly==6.9.0
yfinance==1.5.2
seaborn==0.13.2

# openstatz — interactive offline tearsheet for the momentum portfolio backtest.
#
# MUST be installed with --no-deps. It declares "pandas<3", and installing it
# normally DOWNGRADES this venv's pandas 3.0.0 to 2.3.3, which breaks the
# dashboard data stack (same failure mode as neo-api-client above). The pin is
# conservative -- openstatz 0.4.1 was verified working on pandas 3.0.0:
# ostz.dashboard() and ostz.stats.montecarlo() both run clean.
#
# Its real runtime imports are numpy, pandas, matplotlib, seaborn, plotly and
# yfinance (hard-imported at module load in openstatz/_compat.py even though the
# backtest never fetches remote data). All are listed above or already present,
# and yfinance itself has no upper pandas bound, so it installs normally.
#
#   venv\Scripts\python.exe -m pip install --no-deps openstatz==0.4.1
#
# openstatz==0.4.1
